---
const currentPath = Astro.url.pathname;
---
<div class="sidebar-container">
  <a href="/" class:list={[{selected: currentPath === "/"}]}>Home</a>
  <!-- <a href="/search" class:list={[{selected: currentPath === "/search"}]}>Search</a> -->
</div>

<style>
  .sidebar-container{
    display: flex;
    flex-direction: column;
    inline-size: 18rem;
    overflow-wrap: break-word;
    position: fixed;
  }

  a {
    font-size: 36px;
  }

  a:hover{
    color: red;
  }

  .selected{
    color: red;
    font-family: 'Times New Roman', Times, serif;
    font-style: italic;
  }

  .selected::after{
    content: "\276E";
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    color: red;
    margin-left: .5rem;
  }
</style>